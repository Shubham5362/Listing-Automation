# Seller Hub Frontend

```bash
cd frontend
npm install
npm run dev
```

Production build:

```bash
npm run build
```

The dashboard shell is intentionally API-ready and does not fabricate marketplace metrics. Backend integration will bind the existing `/api/v1/dashboard` and module endpoints in subsequent frontend work.
