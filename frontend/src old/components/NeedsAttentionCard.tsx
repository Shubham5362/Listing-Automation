import React from 'react';
import { AlertTriangle, CheckCircle2, Sparkles } from 'lucide-react';
import { AttentionItem } from '../types';
interface Props { items: AttentionItem[]; onAction: (actionType: string, item: AttentionItem) => void; onAskAi: (prompt: string) => void; onViewAll?: () => void; }
export default function NeedsAttentionCard({ items, onAction, onAskAi, onViewAll }: Props) {
  return <section className="bg-white rounded-xl border border-slate-200 shadow-sm h-full"><div className="px-5 py-4 flex items-center justify-between border-b border-slate-100"><div className="flex items-center gap-2"><h3 className="text-[15px] font-bold">Needs Attention</h3><span className="min-w-5 h-5 px-1 rounded-full bg-slate-100 text-slate-600 text-[10px] font-bold flex items-center justify-center">{items.length}</span></div><button onClick={onViewAll} className="text-xs font-semibold text-indigo-600">View all →</button></div><div className="divide-y divide-slate-100">{items.length ? items.map(item => <div key={item.id} className="px-5 py-3.5 flex items-center justify-between gap-3"><div className="flex items-start gap-3"><span className="px-2 py-0.5 rounded border bg-slate-50 text-slate-600 text-[10px] font-bold uppercase">{item.badge}</span><div><div className="text-[13px] font-bold text-slate-900">{item.title}</div><div className="text-[11px] text-slate-500 mt-0.5">{item.subtitle}</div></div></div><div className="flex gap-2 shrink-0"><button onClick={() => onAction(item.actionType, item)} className="px-3 py-1.5 rounded-md bg-indigo-50 text-indigo-600 border border-indigo-100 text-xs font-semibold">View</button>{item.secondaryAction && <button onClick={() => onAskAi(`Explain this live alert: ${item.title}`)} className="px-3 py-1.5 rounded-md border border-slate-200 text-xs font-semibold text-slate-700"><Sparkles className="w-3 h-3 inline mr-1"/>Ask AI</button>}</div></div>) : <div className="p-8 text-center text-sm text-slate-400"><CheckCircle2 className="w-7 h-7 mx-auto mb-2 text-emerald-500"/>No live alerts reported for this period.</div>}</div></section>;
}
