import React from 'react';
import { createRoot } from 'react-dom/client';
import './styles.css';
import './ai-chat-fullscreen.css';
import MarketplaceWorkspace from './MarketplaceWorkspace';
import MarketplaceChangeWorkspace from './MarketplaceChangeWorkspace';
import OperationsControlCenter from './OperationsControlCenter';
import AISellerAgentWorkspace from './AISellerAgentWorkspace';
import AutofillWorkspace from './AutofillWorkspace';
import ListingIntelligenceWorkspace from './ListingIntelligenceWorkspace';
import DiagnosticWorkspace from './DiagnosticWorkspace';

type Module = { name: string; icon: string; group: string; description: string; tone: string };
const modules: Module[] = [
  { name: 'Dashboard', icon: '⌂', group: 'Overview', description: 'Business command center', tone: 'core' },
  { name: 'Orders', icon: '▣', group: 'Operations', description: 'Unified order operations', tone: 'blue' },
  { name: 'Inventory', icon: '▥', group: 'Operations', description: 'Stock health and availability', tone: 'green' },
  { name: 'Returns', icon: '↩', group: 'Operations', description: 'Returns and customer issues', tone: 'rose' },
  { name: 'Control Center', icon: '⚡', group: 'Operations', description: 'Actions, jobs, approvals and recovery', tone: 'ai' },
  { name: 'Products', icon: '◫', group: 'Catalog', description: 'Central product catalog', tone: 'violet' },
  { name: 'Listings', icon: '▤', group: 'Catalog', description: 'Listing lifecycle', tone: 'violet' },
  { name: 'AI Listing Studio', icon: '✦', group: 'Catalog', description: 'Intelligent generation and contextual variations', tone: 'ai' },
  { name: 'Adaptive Autofill', icon: '✦', group: 'Catalog', description: 'AI marketplace form preparation', tone: 'ai' },
  { name: 'Marketplace Changes', icon: '↻', group: 'Intelligence', description: 'Schema changes and safe adapter updates', tone: 'ai' },
  { name: 'AI Diagnostics', icon: '⌁', group: 'Intelligence', description: 'Root cause analysis and safe fixes', tone: 'ai' },
  { name: 'Pricing', icon: '₹', group: 'Growth', description: 'Pricing intelligence', tone: 'amber' },
  { name: 'Advertising', icon: '◒', group: 'Growth', description: 'Campaign performance', tone: 'orange' },
  { name: 'Finance', icon: '◈', group: 'Finance', description: 'Revenue and profitability', tone: 'cyan' },
  { name: 'Analytics', icon: '⌁', group: 'Intelligence', description: 'Business intelligence', tone: 'indigo' },
  { name: 'Automations', icon: '⚙', group: 'Automation', description: 'Automated workflows', tone: 'slate' },
  { name: 'Notifications', icon: '♢', group: 'Automation', description: 'Alerts and activity', tone: 'slate' },
  { name: 'Marketplaces', icon: '◇', group: 'Configuration', description: 'Amazon and Flipkart status', tone: 'teal' },
];
const API_BASE = (import.meta.env.VITE_API_BASE_URL || '').replace(/\/$/, '');
const api = (path: string) => fetch(`${API_BASE}/api/v1${path}`, { headers: { Accept: 'application/json' } });
const time = (value: string | null) => value ? new Date(value).toLocaleString('en-IN', { dateStyle: 'short', timeStyle: 'short' }) : '—';
type Overview = { kpis: { orders: number; products: number; listings: number; inventory_units: number; low_stock: number; returns: number; pending_jobs: number; pending_approvals?: number }; orders: { by_status: Record<string, number> }; inventory: { total_items: number; units: number; low_stock: number; out_of_stock: number }; catalog: { products: number; listings: number; active_listings: number }; jobs: { by_status: Record<string, number>; recent: { title: string; status: string; created_at: string | null }[] }; approvals?: { pending: number }; marketplaces: { id: number; marketplace: string; status: string; last_sync_at: string | null; last_error: string | null }[]; alerts: { type: string; severity: string; message: string; count: number }[]; activity: { title: string; status: string; created_at: string | null }[] };
function App() {
  const [active, setActive] = React.useState('Dashboard'); const [dark, setDark] = React.useState(() => localStorage.getItem('seller_hub_theme') === 'dark'); const [mobile, setMobile] = React.useState(false); const [aiOpen, setAiOpen] = React.useState(false); const [data, setData] = React.useState<Overview | null>(null); const [error, setError] = React.useState(''); const [loading, setLoading] = React.useState(true); const [updated, setUpdated] = React.useState<Date | null>(null);
  const load = React.useCallback(async () => { try { const response = await api('/operations/overview'); if (!response.ok) throw new Error(`Seller Hub API returned ${response.status}`); setData(await response.json()); setUpdated(new Date()); setError(''); } catch (err) { setError(err instanceof Error ? err.message : 'Unable to load Seller Hub data'); } finally { setLoading(false); } }, []);
  React.useEffect(() => { document.documentElement.dataset.theme = dark ? 'dark' : 'light'; localStorage.setItem('seller_hub_theme', dark ? 'dark' : 'light'); }, [dark]); React.useEffect(() => { load(); const timer = window.setInterval(load, 30000); return () => window.clearInterval(timer); }, [load]); React.useEffect(() => { document.body.style.overflow = aiOpen ? 'hidden' : ''; return () => { document.body.style.overflow = ''; }; }, [aiOpen]);
  const current = modules.find(module => module.name === active) || modules[0]; const go = (name: string) => { setActive(name); setMobile(false); };
  return <div className="app-shell"><a className="skip-link" href="#main">Skip to content</a><aside className={mobile ? 'open' : ''}><div className="brand"><span>◆</span><div><b>Seller Hub</b><small>Personal AI Operations</small></div><button className="mobile-close" onClick={() => setMobile(false)}>×</button></div><nav className="nav-scroll">{['Overview','Operations','Catalog','Growth','Finance','Intelligence','Automation','Configuration'].map(group => <div className="nav-group" key={group}><small>{group}</small>{modules.filter(module => module.group === group).map(module => <button key={module.name} className={active === module.name ? 'active' : ''} aria-current={active === module.name ? 'page' : undefined} onClick={() => go(module.name)}><i>{module.icon}</i>{module.name}</button>)}</div>)}</nav><div className="seller"><small>PERSONAL WORKSPACE</small><strong>My Business</strong><span>● Direct access enabled</span></div></aside><main id="main" tabIndex={-1}><header className="topbar"><button className="mobile-menu" onClick={() => setMobile(true)}>☰</button><div><p className="eyebrow">{current.group.toUpperCase()} · SELLER HUB</p><h1>{active}</h1><p className="muted">{current.description}</p></div><div className="top-actions"><span className="live-status">● Live</span><button onClick={() => setDark(!dark)}>{dark ? '☀' : '◐'}</button><button className="ai-button" onClick={() => setAiOpen(true)}>✦ Ask AI</button></div></header>{error && <div className="errorbar" role="alert">{error} <button onClick={load}>Retry</button></div>}{active === 'Dashboard' ? <Dashboard data={data} loading={loading} updated={updated} go={go} /> : active === 'Marketplaces' ? <MarketplaceWorkspace /> : active === 'Control Center' ? <OperationsControlCenter /> : active === 'Adaptive Autofill' ? <AutofillWorkspace /> : active === 'Marketplace Changes' ? <MarketplaceChangeWorkspace /> : active === 'AI Listing Studio' ? <ListingIntelligenceWorkspace /> : active === 'AI Diagnostics' ? <DiagnosticWorkspace /> : <ModulePage module={current} data={data} loading={loading} updated={updated} />}</main>{aiOpen && <AISellerAgentWorkspace onClose={() => setAiOpen(false)} />}</div>;
}
function Dashboard({ data, loading, updated, go }: { data: Overview | null; loading: boolean; updated: Date | null; go: (name: string) => void }) { const k = data?.kpis; const cards = [['Orders', k?.orders ?? 0, 'All imported orders', '▣'], ['Inventory', k?.inventory_units ?? 0, 'Available units', '▥'], ['Products', k?.products ?? 0, 'Catalog products', '◫'], ['Listings', k?.listings ?? 0, `${k?.listings ? data?.catalog.active_listings : 0} active`, '▤'], ['Returns', k?.returns ?? 0, 'Return requests', '↩'], ['Pending Jobs', k?.pending_jobs ?? 0, 'Queued / running', '⚙'], ['Approvals', k?.pending_approvals ?? data?.approvals?.pending ?? 0, 'Awaiting decision', '✓']]; return <section className="dashboard"><section className="hero-strip"><div><span className="live-dot">● LIVE DATA</span><h2>Business pulse at a glance.</h2><p>One workspace for orders, stock, catalog, marketplaces and operational health.</p></div><div className="refresh-meta"><small>{loading ? 'Loading…' : updated ? `Updated ${updated.toLocaleTimeString('en-IN')}` : 'Waiting for data'}</small><button onClick={() => window.location.reload()}>↻ Refresh</button></div></section><section className="cards">{cards.map(card => <article className="card" key={card[0]}><div className="card-icon">{card[3]}</div><span>{card[0]}</span><strong>{loading ? '—' : card[1]}</strong><small>{card[2]}</small></article>)}</section><section className="grid dashboard-grid"><article className="panel"><PanelHead title="Order status" sub="All marketplace orders"/><Rows items={Object.entries(data?.orders.by_status || {}).map(([status, count]) => ({ a: status.replaceAll('_', ' '), b: count, c: 'orders' }))} empty="No orders imported yet" /></article><article className="panel"><PanelHead title="Needs attention" sub="Live operational alerts"/><Rows items={(data?.alerts || []).map(alert => ({ a: alert.message, b: alert.count, c: alert.severity }))} empty="No active alerts" /></article></section><section className="grid bottom"><article className="panel"><PanelHead title="Marketplaces" sub="Connection and sync status"/>{data?.marketplaces.length ? <div className="rows">{data.marketplaces.map(market => <div key={market.id}><b>{market.marketplace}</b><strong>{market.status}</strong><em>Last sync: {time(market.last_sync_at)}{market.last_error ? ` · ${market.last_error}` : ''}</em></div>)}</div> : <Empty text="No marketplace accounts configured" sub="Amazon and Flipkart can be connected in the Marketplace workspace." />}</article><article className="panel"><PanelHead title="Recent activity" sub="Background operations"/><Rows items={(data?.activity || []).slice(0, 6).map(item => ({ a: item.title, b: item.status, c: time(item.created_at) }))} empty="No background activity yet" /></article></section><div className="screen-shortcuts">{['Control Center','Orders','Inventory','Products','Listings','AI Listing Studio','Marketplaces','Adaptive Autofill','Marketplace Changes','AI Diagnostics'].map(name => <button key={name} onClick={() => go(name)}>{name} →</button>)}</div></section>; }
function ModulePage({ module, data, loading, updated }: { module: Module; data: Overview | null; loading: boolean; updated: Date | null }) { const k = data?.kpis; const cards: Record<string, [string, unknown, string][]> = { Orders: [['Total orders', k?.orders || 0, 'Imported orders'], ...Object.entries(data?.orders.by_status || {}).slice(0, 5).map(([status, count]): [string, unknown, string] => [status.replaceAll('_', ' '), count, 'orders'])], Inventory: [['Available units', data?.inventory.units || 0, 'Sellable stock'], ['Inventory items', data?.inventory.total_items || 0, 'Tracked SKUs'], ['Low stock', data?.inventory.low_stock || 0, 'At reorder level'], ['Out of stock', data?.inventory.out_of_stock || 0, 'Needs replenishment']], Products: [['Products', k?.products || 0, 'Catalog'], ['Listings', k?.listings || 0, 'All listings'], ['Active listings', data?.catalog.active_listings || 0, 'Published active']], Listings: [['Listings', k?.listings || 0, 'All listings'], ['Active', data?.catalog.active_listings || 0, 'Published']], Returns: [['Returns', k?.returns || 0, 'Return requests']], Automations: [['Pending jobs', k?.pending_jobs || 0, 'Queued / running']], Notifications: [['Active alerts', data?.alerts.length || 0, 'Current issues']], Pricing: [['Products', k?.products || 0, 'Pricing scope']], Advertising: [['Products', k?.products || 0, 'Advertising scope']], Finance: [['Orders', k?.orders || 0, 'Finance source']], Analytics: [['Orders', k?.orders || 0, 'Analytics source']] }; const moduleCards = cards[module.name] || [['Seller Hub', 1, 'Workspace']]; return <section className="module-page"><div className={`module-hero ${module.tone}`}><div className="module-mark">{module.icon}</div><div><p className="eyebrow">{module.group.toUpperCase()} CENTER</p><h2>{module.name}</h2><p>{module.description}. This workspace is connected to the live Seller Hub overview.</p></div><span className="connection">● {loading ? 'Refreshing' : `Live · ${updated ? updated.toLocaleTimeString('en-IN') : 'ready'}`}</span></div><section className="cards">{moduleCards.map(([title, value, sub]) => <article className="card" key={title}><span>{title}</span><strong>{loading ? '—' : String(value)}</strong><small>{sub}</small></article>)}</section><div className="module-data"><article className="panel"><PanelHead title="Live workspace data" sub="Read from the central operations API"/>{module.name === 'Notifications' ? <Rows items={(data?.alerts || []).map(alert => ({ a: alert.message, b: alert.count, c: alert.severity }))} empty="No active alerts" /> : module.name === 'Automations' ? <Rows items={(data?.jobs.recent || []).map(job => ({ a: job.title, b: job.status, c: time(job.created_at) }))} empty="No jobs yet" /> : <Rows items={genericRows(module.name, data)} empty="No records available yet" />}</article></div></section>; }
function genericRows(name: string, data: Overview | null) { if (name === 'Inventory') return [['Available units', data?.inventory.units || 0, 'sellable units'], ['Low stock', data?.inventory.low_stock || 0, 'reorder attention'], ['Out of stock', data?.inventory.out_of_stock || 0, 'critical attention']].map(([a,b,c]) => ({a,b,c})); if (name === 'Listings') return [['All listings', data?.catalog.listings || 0, 'records'], ['Active listings', data?.catalog.active_listings || 0, 'published']].map(([a,b,c]) => ({a,b,c})); if (name === 'Orders') return Object.entries(data?.orders.by_status || {}).map(([a,b]) => ({a, b, c: 'orders'})); if (name === 'Returns') return [{a:'Return requests', b:data?.kpis.returns || 0, c:'records'}]; return [{a:'Products in catalog', b:data?.kpis.products || 0, c:'available for this workspace'}]; }
function PanelHead({ title, sub }: { title: string; sub: string }) { return <div className="panelhead"><div><h2>{title}</h2><p>{sub}</p></div></div>; }
function Rows({ items, empty = 'No data available yet' }: { items: { a: unknown; b: unknown; c: unknown }[]; empty?: string }) { return items.length ? <div className="rows">{items.map((item, index) => <div key={`${String(item.a)}-${index}`}><b>{String(item.a)}</b><strong>{String(item.b)}</strong><em>{String(item.c)}</em></div>)}</div> : <Empty text={empty} />; }
function Empty({ text, sub }: { text: string; sub?: string }) { return <div className="empty"><strong>{text}</strong>{sub && <small>{sub}</small>}</div>; }
createRoot(document.getElementById('root')!).render(<App />);