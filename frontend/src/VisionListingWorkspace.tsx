import React from 'react';
const API=(import.meta.env.VITE_API_BASE_URL||'').replace(/\/$/,'');
export default function VisionListingWorkspace(){const [productId,setProductId]=React.useState('');const [result,setResult]=React.useState<any>(null);const [busy,setBusy]=React.useState(false);const scan=async()=>{if(!productId)return;setBusy(true);try{const r=await fetch(`${API}/api/v1/listing-validation/scan`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({product_id:Number(productId)})});setResult(await r.json())}finally{setBusy(false)}};return <section className="module-page"><div className="module-hero ai"><div className="module-mark">◉</div><div><p className="eyebrow">INTELLIGENCE CENTER</p><h2>AI Listing Validator</h2><p>Vision quality, listing content, SEO, attributes, compliance and variation health in one scan.</p></div><span className="connection">● Vision ready</span></div><div className="panel" style={{padding:24}}><div style={{display:'flex',gap:12,alignItems:'center',flexWrap:'wrap'}}><input aria-label="Product ID" placeholder="Product ID" value={productId} onChange={e=>setProductId(e.target.value)} /><button className="ai-button" onClick={scan} disabled={busy}>{busy?'Scanning…':'Run AI Validation'}</button></div>{result&&<div style={{marginTop:24}}><h3>Listing Health: {result.health_score}/100</h3><p>Status: <b>{result.status}</b> · Images: {result.image_score} · SEO: {result.seo_score} · Attributes: {result.attributes_score}</p><div className="rows">{(result.findings||[]).map((f:any,i:number)=><div key={i}><b>{f.code}</b><strong>{f.severity}</strong><em>{f.message}</em></div>)}</div></div>}</div></section>}
